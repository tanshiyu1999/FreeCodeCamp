/*
Queue
First in first out

this.print => console.log something
this.enqueue => push item
this.dequeue => shift item
this.front => return front of array
this.size => collection.length
this.isEmpty => return boolean whether queue is empty (array is empty)
*/

var q = new Queue();


/*
Priority Queue
You pass the element and priority of element into the Queue
If priority same, same queue, however if priority is higher, it's passed to the front of queue
*/

var pq = new PriorityQueue();
